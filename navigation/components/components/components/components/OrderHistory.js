// components/OrderHistory.js
import React from 'react';
import { View, Text } from 'react-native';

const OrderHistory = () => {
  return (
    <View>
      <Text>Order History</Text>
      {/* Add functionality to display order history */}
    </View>
  );
};

export default OrderHistory;
